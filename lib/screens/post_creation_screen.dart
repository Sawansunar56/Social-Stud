import 'package:flutter/material.dart';

class PostCreation extends StatelessWidget {
  const PostCreation({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Column(
        children: [Text("Hello from post")],
      ),
    );
  }
}
